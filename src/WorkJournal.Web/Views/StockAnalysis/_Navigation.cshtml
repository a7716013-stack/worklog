@{ var isSwing = ViewContext.RouteData.Values["action"]?.ToString() == "Swing"; }
<nav class="d-flex flex-wrap gap-2 mb-4" aria-label="分析介面切換">
    <a class="btn @(!isSwing ? "btn-forest" : "btn-light")" asp-controller="StockAnalysis" asp-action="Index" aria-current="@(!isSwing ? "page" : null)">個股分析</a>
    <a class="btn @(isSwing ? "btn-forest" : "btn-light")" asp-controller="StockAnalysis" asp-action="Swing" aria-current="@(isSwing ? "page" : null)">波段分析</a>
</nav>
